diff --git a/node_modules/solarch/dist/apis/admin_auth.js b/node_modules/solarch/dist/apis/admin_auth.js
index 84250b6..e2b529d 100644
--- a/node_modules/solarch/dist/apis/admin_auth.js
+++ b/node_modules/solarch/dist/apis/admin_auth.js
@@ -72,7 +72,10 @@ function registerAdminAuthRoutes(app, router) {
                 (0, lockout_1.recordFailedAttempt)(`admin:${identity.toLowerCase()}`);
                 return res.status(400).json((0, api_errors_1.createApiError)(400, 'UNAUTHORIZED', 'Invalid credentials.'));
             }
-            const valid = await (0, crypto_1.verifyPassword)(password, row.passwordHash);
+            const valid = await (0, crypto_1.verifyPassword)(
+                password,
+                row.passwordHash ?? row.passwordhash
+                );
             if (!valid) {
                 (0, lockout_1.recordFailedAttempt)(`admin:${identity.toLowerCase()}`);
                 return res.status(400).json((0, api_errors_1.createApiError)(400, 'UNAUTHORIZED', 'Invalid credentials.'));
diff --git a/node_modules/solarch/dist/apis/auth_flows.js b/node_modules/solarch/dist/apis/auth_flows.js
index 963d3f8..d2191ad 100644
--- a/node_modules/solarch/dist/apis/auth_flows.js
+++ b/node_modules/solarch/dist/apis/auth_flows.js
@@ -50,7 +50,7 @@ function registerPasswordResetRoutes(app, router) {
             const record = new record_1.RecordModel(collection.id, collection.name, row);
             const token = await app.createPasswordResetToken(record.id, `collection_${collection.id}`, 1);
             const now = new Date().toISOString();
-            await app.db().execute(`UPDATE _r_${collection.id} SET lastResetSentAt = ? WHERE id = ?`, [now, record.id]);
+            await app.db().execute(`UPDATE _r_${collection.id} SET "lastResetSentAt" = ? WHERE id = ?`, [now, record.id]);
             try {
                 const settings = app.settings();
                 if (settings.smtp.host) {
@@ -103,7 +103,7 @@ function registerPasswordResetRoutes(app, router) {
                 return res.status(400).json((0, api_errors_1.createApiError)(400, 'VALIDATION_FAILED', 'Invalid token.'));
             }
             const passwordHash = await app.hashPassword(password);
-            await app.db().execute(`UPDATE _r_${collection.id} SET passwordHash = ?, verified = 1 WHERE id = ?`, [passwordHash, row.id]);
+            await app.db().execute(`UPDATE _r_${collection.id} SET "passwordHash" = ?, verified = 1 WHERE id = ?`, [passwordHash, row.id]);
             res.json({ code: 200, message: 'Password reset successfully.' });
         }
         catch (err) {
@@ -130,7 +130,7 @@ function registerVerificationRoutes(app, router) {
             const record = new record_1.RecordModel(collection.id, collection.name, row);
             const token = app.generateJWT({ id: record.id, type: 'verifyEmail', collectionId: collection.id }, app.getJwtSecret(), '2h');
             const now = new Date().toISOString();
-            await app.db().execute(`UPDATE _r_${collection.id} SET lastVerificationSentAt = ? WHERE id = ?`, [now, record.id]);
+            await app.db().execute(`UPDATE _r_${collection.id} SET "lastVerificationSentAt" = ? WHERE id = ?`, [now, record.id]);
             try {
                 const settings = app.settings();
                 if (settings.smtp.host) {
diff --git a/node_modules/solarch/dist/apis/record_auth.js b/node_modules/solarch/dist/apis/record_auth.js
index d31fba7..862a76c 100644
--- a/node_modules/solarch/dist/apis/record_auth.js
+++ b/node_modules/solarch/dist/apis/record_auth.js
@@ -88,7 +88,7 @@ function registerAuthRoutes(app, router) {
                 (0, lockout_1.recordFailedAttempt)(lockoutKey);
                 return res.status(400).json((0, api_errors_1.createApiError)(400, 'UNAUTHORIZED', 'Invalid login credentials.'));
             }
-            const passwordHash = row.passwordHash;
+            const passwordHash = row.passwordHash ?? row.passwordhash;
             const valid = await (0, crypto_1.verifyPassword)(password, passwordHash);
             if (!valid) {
                 (0, lockout_1.recordFailedAttempt)(lockoutKey);
@@ -108,7 +108,7 @@ function registerAuthRoutes(app, router) {
             record.hide('lastResetSentAt');
             record.hide('lastVerificationSentAt');
             const token = app.generateJWT({ id: record.id, type: 'auth', collectionId: collection.id }, app.getJwtSecret(), '720h');
-            await app.db().execute(`UPDATE ${qt} SET lastLoginAt = ? WHERE id = ?`, [new Date().toISOString(), record.id]);
+            await app.db().execute(`UPDATE ${qt} SET "lastLoginAt" = ? WHERE id = ?`, [new Date().toISOString(), record.id]);
             res.json({ token, record: record.toJSON() });
         }
         catch (err) {
@@ -247,7 +247,7 @@ function registerAuthRoutes(app, router) {
             const record = new record_1.RecordModel(collection.id, collection.name, recordRow);
             const token = app.generateJWT({ id: record.id, type: 'auth', collectionId: collection.id }, app.getJwtSecret(), '720h');
             await app.db().execute(`DELETE FROM _otps WHERE id = ?`, [otpId]);
-            await app.db().execute(`UPDATE ${(0, sql_safe_1.quoteIdentifier)(`_r_${collection.id}`)} SET lastLoginAt = ? WHERE id = ?`, [new Date().toISOString(), record.id]);
+            await app.db().execute(`UPDATE ${(0, sql_safe_1.quoteIdentifier)(`_r_${collection.id}`)} SET "lastLoginAt" = ? WHERE id = ?`, [new Date().toISOString(), record.id]);
             record.hide('passwordHash');
             record.hide('lastResetSentAt');
             record.hide('lastVerificationSentAt');
@@ -396,7 +396,7 @@ function registerAuthRoutes(app, router) {
                 record.hide('lastResetSentAt');
                 record.hide('lastVerificationSentAt');
                 const authToken = app.generateJWT({ id: payload.id, type: 'auth', collectionId: collection.id }, app.getJwtSecret(), '720h');
-                await app.db().execute(`UPDATE ${(0, sql_safe_1.quoteIdentifier)(`_r_${collection.id}`)} SET lastLoginAt = ? WHERE id = ?`, [new Date().toISOString(), payload.id]);
+                await app.db().execute(`UPDATE ${(0, sql_safe_1.quoteIdentifier)(`_r_${collection.id}`)} SET "lastLoginAt" = ? WHERE id = ?`, [new Date().toISOString(), payload.id]);
                 return res.json({ verified: true, token: authToken, record: record.toJSON() });
             }
             res.json({ verified: true });
